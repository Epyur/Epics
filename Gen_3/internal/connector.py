from Gen_3.internal.xlsx import *
from Gen_3.internal.xls import *

# inc_df_merged = inc_df.merge(ekn_df, how='left', on=["ekn"])
# inc_df_merged_comb = inc_df_merged.merge(flamability(), how='left', on=["ID"])
# inc_df_merged_comb_flam = inc_df_merged_comb.merge(flam_df_gen, how='left', on=["ID"])
